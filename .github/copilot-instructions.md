# Workspace instructions

- Keep the tutorial broken into small, independently testable Godot tasks.
- Prefer GDScript examples that work in Godot 4.x.
- Keep documentation builds passing with `mkdocs build --strict`.
- Keep media references in `docs/assets` and use ASCII-authored Markdown.
